size = [38,40,42,44]
b = bytearray(size)      #commands are write in python are used to asu symbol only 

print(type(b))           #python are everything object only

size1 = {38,40,40}
fs=frozenset(size1)
print(type(fs))